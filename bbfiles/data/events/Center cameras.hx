var bg:FlxSprite;

function onEvent(event)
{
	if (event.event.name == "Center cameras")
	{
		var params:Array = event.event.params;
		var xOffset:Float;

		if (params[0])
		{
			trace(dad.camyoffset);
		}
		else
		{
			return;
		}
    }
}